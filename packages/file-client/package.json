{
  "name": "@spring/file-client",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run ./dist/index.js",
    "dev": "bun run ./src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "lint": "bun run eslint --ext .ts .",
    "test": "bun test"
  },
  "peerDependencies": {
    "typescript": "^5.5.0"
  }
}
